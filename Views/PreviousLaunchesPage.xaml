<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DustsSpaceLaunchTracker.Views.PreviousLaunchesPage"
             xmlns:vm="clr-namespace:DustsSpaceLaunchTracker.ViewModels"
             xmlns:cards="clr-namespace:DustsSpaceLaunchTracker.Views.Cards"
             x:DataType="vm:PreviousLaunchesViewModel"
             Title="Previous">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*,Auto"
          Padding="12"
          RowSpacing="8">

        <VerticalStackLayout Grid.Row="0" Spacing="2">
            <Label Text="{Binding Title}"
                   FontSize="22"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />
            <Label Text="{Binding StatusText}"
                   FontSize="12"
                   TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}"
                   HorizontalOptions="Center"
                   IsVisible="{Binding StatusText, Converter={StaticResource IsNotNullOrEmptyConverter}}" />
        </VerticalStackLayout>

        <Grid Grid.Row="1"
              ColumnDefinitions="*,Auto,Auto"
              ColumnSpacing="6">
            <SearchBar Grid.Column="0"
                       Placeholder="Search previous launches…"
                       Text="{Binding SearchText}"
                       SearchCommand="{Binding SearchCommand}" />
            <Button Grid.Column="1"
                    Text="Go"
                    Command="{Binding SearchCommand}"
                    Padding="12,0" />
            <Button Grid.Column="2"
                    Text="✕"
                    Command="{Binding ClearSearchCommand}"
                    Padding="12,0" />
        </Grid>

        <Picker Grid.Row="2"
                Title="Status filter"
                ItemsSource="{Binding StatusFilters}"
                ItemDisplayBinding="{Binding Name}"
                SelectedIndex="{Binding SelectedStatusIndex}" />

        <Border Grid.Row="3"
                IsVisible="{Binding HasError}"
                StrokeThickness="1"
                Padding="10"
                BackgroundColor="{AppThemeBinding Light=#FFF5F5, Dark=#3A1F1F}">
            <Border.Stroke>
                <SolidColorBrush Color="#E57373" />
            </Border.Stroke>
            <VerticalStackLayout Spacing="6">
                <Label Text="{Binding ErrorMessage}"
                       TextColor="#C62828"
                       FontSize="13"
                       HorizontalTextAlignment="Start" />
                <Label Text="{Binding ErrorDetail}"
                       FontSize="10"
                       TextColor="{AppThemeBinding Light=#6D4C41, Dark=#E0E0E0}"
                       IsVisible="{Binding ShowErrorDetail}"
                       LineBreakMode="WordWrap" />
                <FlexLayout Wrap="Wrap">
                    <Button Text="Details"
                            Command="{Binding ToggleErrorDetailCommand}"
                            IsVisible="{Binding HasErrorDetail}"
                            FontSize="12"
                            Padding="10,4"
                            Margin="0,0,8,0" />
                    <Button Text="Copy"
                            Command="{Binding CopyErrorCommand}"
                            FontSize="12"
                            Padding="10,4"
                            Margin="0,0,8,0" />
                    <Button Text="Diagnostics"
                            Command="{Binding OpenDiagnosticsCommand}"
                            FontSize="12"
                            Padding="10,4" />
                </FlexLayout>
            </VerticalStackLayout>
        </Border>

        <RefreshView Grid.Row="4"
                     Command="{Binding LoadLaunchesCommand}"
                     IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Launches}"
                            RemainingItemsThreshold="5"
                            RemainingItemsThresholdReachedCommand="{Binding LoadMoreLaunchesCommand}"
                            EmptyView="No previous launches found."
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:LaunchListItemViewModel">
                        <Grid>
                            <cards:LaunchCard />
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.OpenLaunchCommand}"
                                    CommandParameter="{Binding .}" />
                            </Grid.GestureRecognizers>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.Footer>
                    <VerticalStackLayout Padding="0,12" Spacing="6" HorizontalOptions="Center">
                        <ActivityIndicator IsRunning="{Binding IsLoadingMore}"
                                           IsVisible="{Binding IsLoadingMore}"
                                           Color="DodgerBlue"
                                           HeightRequest="32"
                                           WidthRequest="32" />
                        <Label Text="Scroll for more…"
                               FontSize="11"
                               TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}"
                               IsVisible="{Binding HasMoreItems}"
                               HorizontalOptions="Center" />
                        <Label Text="End of list"
                               FontSize="11"
                               TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}"
                               IsVisible="{Binding ShowEndOfList}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </CollectionView.Footer>
            </CollectionView>
        </RefreshView>

        <Label Grid.Row="5"
               Text="Data from The Space Devs API"
               FontSize="11"
               TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}"
               HorizontalOptions="Center" />
    </Grid>
</ContentPage>
