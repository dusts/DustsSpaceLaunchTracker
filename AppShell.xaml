<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DustsSpaceLaunchTracker.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DustsSpaceLaunchTracker.Views"
    Title="Dusts Space Launch Tracker">

    <ShellContent
        Title="Upcoming"
        ContentTemplate="{DataTemplate local:UpcomingLaunchesPage}"
        Route="UpcomingLaunches" />

</Shell>
